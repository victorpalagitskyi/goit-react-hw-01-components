export const Profile = ({ user: { username, tag, location, avatar, stats } }) => {
    return <div>
<Profile
  username={username}
  tag={tag}
  location={location}
  avatar={avatar}
  stats={stats}
/>
    </div>
};